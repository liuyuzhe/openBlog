<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.strongliu.blog.dao.UserDao">

    <insert id="insert" parameterType="User">
        INSERT INTO user_table
        (id, user_name, user_password, user_nickname, user_email, user_avatar_url, user_register_time, user_status)
         VALUES
         (#{id}, #{name}, #{password}, #{nickname}, #{email}, #{avatar_url}, #{register_time}, #{status})
    </insert>

    <select id="selectById" parameterType="String" resultType="User">
        SELECT * FROM user_table
        WHERE id=#{id}
    </select>

    <update id="update" parameterType="User">
        UPDATE user_table
        SET user_name=#{name}, user_password=#{password}, user_nickname=#{nickname}, user_email=#{email}, user_avatar_url=#{avatar_url}, user_register_time=#{register_time}, user_status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="String">
        DELETE FROM user_table
        WHERE id=#{id}
    </delete>

</mapper>